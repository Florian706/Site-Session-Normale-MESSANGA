// Affiche un message dans la console au chargement
document.addEventListener('DOMContentLoaded', () => {
    console.log("Bienvenue sur le site de l’Entreprise MESSANGA !");
  });
  
  // Exemple d’interaction simple
  window.addEventListener('scroll', () => {
    console.log("Défilement de la page détecté !");
  });